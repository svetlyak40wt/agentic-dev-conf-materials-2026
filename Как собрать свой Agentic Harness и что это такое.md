Всем привет, да, сегодня у нас будет, поговорим с вами про агентик-харнес или айджент-харнес, вообще, в принципе, что это такое и как его собрать, из каких этапов он состоит. Пару слов обо мне, чем вообще я занимаюсь. Так уж вышло, что, оказывается, я строю платформу для айджент-харнесов, да, термин недавно появился, но вот этим, собственно, и занимаемся.

Делаем горизонтальную платформу для построения мультиагентных организаций. И так уж вышло тоже, второй мой продукт, он, наоборот, больше про то, как сделать вертикального агента, который в своей такой обвязке может, помогая строителям, делать методы для стройки. Отлично.

Прежде, чем перейдем к харнесам, вообще, скажите, какими агентами вы пользуетесь? Вот можете прямо назвать вслух с зала. Клодом? Так, только клодом, да? Gitverse, там, Гигачат, я почему не слышу, ничего такого нет, да? Виталик, там, я не знаю, что у Яндекса, внутри какие-то тоже, наверное, есть. Отлично. 

Скорее всего, вы назвали что-то из этого, клод-код, курсор, да, опен-код, возможно, кодекс. Но есть одна большая неувязочка, этот список агент-харнесов, да, я буквально загуглил топ-10 агент-харнесов и посмотрел, какие из них есть, вывел сюда. Получается, ровно в этот же список мы можем добавить точно те же самые гермесы, опен-кло и все, что сейчас релизится.

Отсюда вопрос, что такое же на самом деле агент-харнес? Ну, первым делом, что я сделал, я решил пойти, надо спросить самого Клода, как он считает, что такое агент-харнес. И, в принципе, Клод мне так и ответил, когда я спросил, код-код, ты агент-харнес? Он сказал, да, я агент-харнес. Но, опять же, я его спросил, является ли он и агентом или нет, он тоже сказал мне, что он является и агентом.

В общем, он на все со мной соглашался, я от него так и не смог получить ответа утвердительного, что же такое, в чем это отличается, агент-харнес от того самого агента. Может быть, это модель? Давайте посмотрим. Здесь интересная статистика, где где-то приходит гитхаб, для своего копилота они сделали, как раз, отличие их сей лайки и гоняли это все на разных моделях.

Ну, например, стонет, там, по-моему, 4.6-4.7 указан. Точно так же кодекс. Где-то задачи они делали лучше, где-то хуже.

Точно так же у них есть статистика по количеству затрачиваемых токенов. Кстати, интересно, что токенов они почти везде ели больше. Получается, что это немного не совсем про модель.

Вообще, впервые термин «harness», возможно, он относительно старый, но в рамках e-агентов он начал использоваться и появляться вот в феврале 2026 года в статье от чата GPT от OpenAI, где они дали задачу своему кодексу, чтобы он спроектировал с них сразу с нуля какое-то приложение, и они должны были вообще не вмешиваться. И, как говорится, здесь понеслось. Через месяц похожая статья вышла у Клода про харнесы. 

В апреле уже почти в каждом блоге начал появляться. Здесь блог Мартина Фаулера. Кстати, максимально советую.

Тоже интересно, про агентские харнесы именно для кодинга используются. И, в принципе, понятно. Если мы спросим, что такое вообще агент, сейчас уже теперь Google начнет нам выдавать примерно такое, что агент — это некоторый харнес и модель.

Мне, в принципе, очень нравится такой подход, когда мы агента называем каким-то сотрудником. Ну, давайте представим, что это сотрудник, он берет наши задачи, что-то делает и выдает нам некоторый результат или артефакт. Отлично, у нас есть сотрудник, мы знаем, что модель — это его мозг, какой бы он, собственно, ни был, китайский, не китайский, американский, российский, неважно, модель — это его мозг.

Но что же такое харнес? Отлично, давайте мы, в принципе, в такой же парадигме и пойдем, мы наймем нашего сотрудника, найдем ту самую заветную карточку зарубежную и возьмем 20 долларов, и заключим с ним job offer, возьмем его на работу и пойдем делегировать ему задачи. Вот у вас наступает ваш первый рабочий день. Вашему сотруднику приходит некоторая задача.

Что происходит дальше? Ну, мы ожидаем от него некоторое решение. В принципе, логично. Мы сотрудника наняли, можем ожидать от него решения.

Кстати, а есть те, кто не смог из клода или из кодекса получить какой-то хороший результат? Кто прям недовлетворен его работой? Отлично. Вы ему так и напишите, скажи, я тебя уволю, ты свои 20 долларов отрабатывай, иначе я тебя уволю. Скажи мне, что ты должен делать, чтобы я тебя не уволил.

Есть еще такой хак, можно сказать ему, у меня остались последние 100 долларов, и если ты не начнешь принести денег, я тебя уволю. Он скажет, если у тебя остались последние 100 долларов, то потратить на подписку не самая хорошая идея. В общем, отлично. 

Как и любой другой работник, обычно мы ему делегируем задачи, он начинает что-то думать, что-то делать, и, в принципе, так по циклу, пока не придет к нам с результатом и не скажет, я сделал камни. В принципе, это выглядит примерно так. Единственное различие от обычного сотрудника, у агента появляется тот самый event log, потому что мы помним, что агент, поскольку у него мозг принимает токены, отдает токены, нам необходимо все-таки немного помнить историю того, что он делал, и у нашего сотрудника появляется event log, сообщение пользователя, месседжи, что он сделал, куда он возвращается, и нам необходимо где-то тогда запускать ту самую его думалку.

В принципе, неплохо. Где можно запустить думалку? Ну, понятно, на нашем устройстве. В принципе, многие уже так и делают.

Клод, кодекс, и наш event log будет появляться у агента в таких вот интересных файликах. Как правило, все локальные агенты кладут его примерно в JSON-L. Кстати, здесь тоже интересный факт.

Если вы кто-то пробовал взять всю свою историю, сколько вы общения с Клодом, и попросить его кластеризовать. А вот не опуская руки, теперь оставьте тех, кто был доволен результатом. Вот, отлично.

Мне, в принципе, очень это понравилось, что я попросил тоже точно так же кодекса или Клода, точно не помню, здесь кодекс указан, но, по-моему, я все-таки Клода просил, чтобы он взял, посмотрел все, что я делаю, и составил некоторую LLM-вики, ну или просто некоторую базу знаний, вообще всех подходов, все, что мне нравится и не нравится. И это очень интересный трюк. Обязательно попробуйте.

У меня, по-моему, было около 500 мегабайт данных. Это только в Клоде. В кодексе еще там сколько-то.

Конечно, надо все это еще, по-моему, дорабатывать, но очень такой интересный подход. Отлично. Можем, да, гонять на нашем компьютере в CLI. 

Многие разработчики, скорее всего, я думаю, очень любите CLI, кто-то, может быть, меньше, но тем не менее. Либо в десктопе. И, разумеется, точно так же, раз есть какое-то устройство, мы можем наш агентский цикл гонять где-то на сервере. 

Но, поскольку мы знаем, что агенты могут работать над задачами долго, нам все-таки хотелось бы, чтобы это все было надежно. Потому что, можно представить такую ситуацию. Наш агент запустился, начал производить те самые event-логи и в какой-то момент подзалип. 

Ну, с кем не бывает. Отвлекся на TikTok. Как и любой сотрудник, он всегда может подзалипнуть и вообще забыть, за что ему платят деньги, за что мы его наняли.

И отвлечем другую задачу. В принципе, от такого сотрудника мы ожидаем, что он продолжит свою задачу без нашего какого-то дополнительного пинка и продолжит ее выполнять. Отсюда сразу возникает самое главное, что необходимо нашему харнесу. 

Это некоторый рантайм. И, как и с любым рантаймом, с ним может пойти что-то не так. Что же может пойти не так? Так уж вышло, что наш сотрудник такой довольно интересный.

Он гоняться может почти на любом устройстве. Может гоняться у нас, может гоняться где-то в сервере. А мозг у него почему-то вынесен где-то в сторону.

Вот такой вот нерадивый сотрудник. Этот мозг у него может отпасть. У него могут отпасть иногда руки, которыми он что-то делает. 

У него может отпасть даже тот самый доступ к своему эвентлогу. И отсюда сразу возникает вопрос, что от нашего рантайма такого что-то необходимо. Как и в любой микросервисной архитектуре, мы ожидаем, что у данного рантайма будет durability. 

То есть, если что-то упало, оно потом продолжит работать с этого же места. Также, если где-то какой-то сервис недоступен, что мы к нему сможем постучаться и заново его запустить, получить снова от него результат. Ну и разумеется, мы хотим, помимо того, чтобы он был надежный, мы хотим еще и выбирать, где эти данные, где та самая его работа будет происходить. 

Это такая очень прикольная особенность нашего агента. Отлично. Какие мы можем взять рантаймы? Ну, можем пойти и сами попробовать написать этот рантайм руками, либо взять какой-то готовый фреймворк, ленграф, тоже неплохой рантайм.

У них есть чекпойнтеры для того самого запоминания, что где был event log. Можно взять фреймворк мастера, это отверстие, которое написано в TypeScript. Но мне на самом деле этот фреймворк не очень понравился, потому что он еще в бета-версии, и очень часто у нас что-то с ним все лагало в продакшене, поэтому его не особо советую.

Либо можем пойти и взять какой-то готовый оркестратор. Когда мы приступали к разработке своей агентской платформы, мы рассмотрели на тот момент, почти год назад, несколько разных оркестраторов. Первый, который был, ну разумеется в open-source оркестраторов, мне очень понравился Dapr, и их тоже оркестратор для агентов Dapr Agents, но не совсем понравились те подходы, которые у них были на тот момент, потому что они все еще продолжали относиться вообще ко всем агентам, как будто бы это дерминированный сервис, микросервисная архитектура, и в целом с ней так работать.

Дальше мы посмотрели на такой оркестратор Restate, он мне тоже в принципе очень импонирует, мне нравится их подход, и в принципе очень сильная команда стоит за разработкой. Если я правильно помню, фаундер Restate это один из инженеров, который разработал Kafka, и в принципе, кстати, многие подходы они там тоже с масштабируемостью там используют. Но с Restate есть небольшая проблема в том, что не самая, как скажем, не самая большая комьюнити, и в принципе у них до сих пор не было релизной версии, поэтому мы это маленько тоже решили от себя подальше отодвинуть и взять что-то более надежное, мы взяли Temporal, я постоянно неправильно ставлю ударение этого сервиса, но тем не менее.

Отлично, open-source, Apache 2.0 лицензия, можем надежно гонять наши циклы, все классно, но сразу возникает вопрос, нам до бесконечности, что ли эти циклы гонять, потому что отлично, циклы есть и в инты идут, агент что-то производит или тот самый Harness, и надо как-то этот цикл останавливать. Ну как его останавливать? Ну в первую очередь мы можем остановить, когда задача была выполнена. Звучит как бы логично, поэтому сразу когда останавливаться, ну здесь сейчас уже модно, если вы пользуетесь кодом, тот самый goal, в принципе этот goal можно сделать и самому без проблем, на любой обвязке вокруг агента.

Можно сделать цикл, когда агент будет гонять что-то в цикле, пока в общем какой-то Checker, Verifier, который его проверяет, не решит, что задача выполнена. Либо мы можем сделать более детерминированные критерии останова. Например, когда какой-то критерий соблюден, если предыдущие они больше рассчитаны именно на семантику, когда агент сам понял, что надо остановиться, то вторые мы можем на уровне уже как бы инфраструктуры или на уровне вот этой обвязки для него останавливать. 

Например, ну конечно, это все наши линтеры, тесты, чем больше таких детерминированных верификаторов, сделанные задачами, мы с вами настроим, тем лучше. Отлично. Получается, Runtime у нас состоит из двух частей.

Первая – это некоторая инфраструктура, на которой наш агент должен запускаться надежно, что должен дорабатывать, и некоторая семантика, что агент может проверять сам себя, он должен работать, пока не достигнет какой-то цели. Мне вообще нравится этот подход, что любой Harness, по сути, это некоторая инфраструктура и семантика. И в конце как раз тоже маленько будет пару слов скажу, почему это особенно важно.

Отлично. Мы теперь знаем, что Harness как минимум состоит из Runtime. Но, поскольку у нашего агента есть некоторый мозг, он что-то может делать, но мы же не хотим от него получать только текст.

Надо дать ему руки. Здесь сразу на первое место выходят те самые Tools. У любого агента, у любого агента Harness есть возможность вызывать Tools. 

Ну, это по дефолту, везде идет это. Давайте мы будем дать ему читать файл, давайте мы дадим ему возможность писать файл, ну и все-таки в этом файле маленько что-то поискать, потому что иначе какой смысл от такого агента, который будет все только читать и писать. Как правило, еще часто, если посмотреть Cloud Code, у него есть еще Web Search, и, в принципе, многие выделяют это отдельно, потому что агенту просто будет проще найти.

Есть MCP, которые многие настраивают, и, самое важное, есть Bash. Я думаю, в принципе, наверное, можно вообще все предыдущее убрать и оставить здесь только Bash, и сказать агенту, вот, ходи в Bash и только Башем и пользуйся. И сразу возникает такая проблема, когда вы своему сотруднику даете доступ в Bash и даете полную возможность что-то делать, ну, я думаю, что любой Team Lead своего стажера видит примерно вот так, когда вы ему доверили задачу.

Поэтому в общем, сразу возникают определенные проблемы. Может показаться, что таких проблем не было, но нет, конечно. Постоянно возникают.

В одном месте E-агент... мне, в принципе, вообще очень нравится, как сейчас модно стало говорить, что не кто-то накосячил, а у нас E-агент пошел, удалил, у нас E-агент взял, стер. Ну, конечно, это же E-агент накосячил, а не разработчик, который дал ему доступ в продовую базу. Да, не суть важна, да, вот, много кейсов, там E-агент снес стартап очередную на 2,5 миллиона долларов, у реплита E-агент снес прод-базу данных одного из их кастомеров, причем они об этом сказали только через год. 

Ну, видимо, когда все-таки смогли как-то восстановить отношение своим клиентам. Или, вот, например, кейс, где E-агент за 9 секунд снес продовую базу и продолжал утверждать, что это делал не я. Ну, я говорю, в принципе, как стажер. Дали ему в руки гранату, да, он пошел все разносить.

Поэтому у меня сразу в голове возникает вот, что к нашему агенту мы должны относиться вот примерно как на этой фотке, да, нового инженера, который впустили в прод-базу. Я, в принципе, даже сам много раз сталкивался, ну, я такой довольно евангелист по всем этим агентским вещам, и я не боюсь, I'm a bad boy, давайте ему смело кидать какие-нибудь креды, токены, в общем, пусть он там делает, главное задачу сделает. Только надо токен не забыть назвать. 

И здесь недавно на днях я ему закинул токен, чтобы в тайм-вебе проверил возможность подъема какого-то сервера. И еще сказал ему кодексу, говорю, код-код справился, так что я ожидаю, что тоже справишься. Он долго крутился, в общем, ничего не смог сделать, поднял мне вместо этого все возможные сервера, которые он смог поднять в тайм-вебе. 

Хорошо, что я заметил, что он пошел и поднял, да, и сразу вдруг чек очень сильно увеличился. Но тем не менее, надо четко понимать, какие задачи выдаются агенту. Логично. 

В принципе, когда мне решили доверить задачу, возникает сразу вопрос, ну, не мне, а агенту. Как обезопасить всех вокруг от меня? Ну, и логично, давайте поставим его в клетку. Что еще делать? Других-то выходов нету. 

И здесь на первую роль выходит тот самый сэндбоксинг для агентов. Надо понимать, что, во-первых, да, сэндбокс это способ ограничения blast ratio и возможности агенту сделать что-то в непоправимой инфраструктуре вашей, либо локальной, либо если вы запускаете его так смело на сервере. Но второй момент, что сэндбокс это способ расширения возможностей, ну и, в принципе, баш вашего агента без переписывания каких-то специальных правил. 

Почему я на этом делаю большой акцент? Когда мы разрабатывали агента для констрактива нашего сервиса, мы довольно четко ожидали, что будет фиксированный workflow, фиксированный какой-то UI, пользователи будут заходить, грузить сметы, мы будем видеть результаты и что происходит дальше. А дальше происходит следующее. А можно выгрузить смету без колонки 1? А можно добавить еще 5 других колонок? А давайте мы будем считать в попугаях, а не в метрах. 

А потом давайте вот вам 10 смет, давайте вы все сравните и найдете финальный результат. И интересно становится то, что в принципе пользователи они уже настолько привыкли к агентам, что они не ожидают от нас просто фиксированных агентов, они ожидают такие полноценные харнесы, чтобы агент в них крутился и выполнял те задачи, которые они им поставляют. Здесь вот интересно, как мы с этим столкнулись, что стендбоксы мы и не хотели изначально туда прикручивать, но пришлось.

Отлично. Получается, что харнес это некоторый рантайм, это какие-то определенные инструменты, которые есть, MCP, там баш, стендбокс, в общем, все то, что вы смогли ему дать. Но вот сразу переходим дальше. 

В принципе, вспомните свое ощущение, когда вы приходите на новую работу или когда вы поправляетесь в новый проект, или когда вам необходимо изучить какой-то новый навык или умение. У меня первая мысль всегда примерно вот такая. Что делать вообще? Ладно, не буду слух проносить, как сходить в туалет? И сразу непонятно куда двигаться? Куда смотреть? Что вообще от меня хотят? Потом, в принципе, подумав маленько, я вспоминаю, а, точно, мне же платили за это деньги, надо же что-то сделать.

Меня ожидает артефакт, ожидает результат. И в голове сразу возникает та самая должностная инструкция, которая звучит примерно, хорошо делай, плохо не делай, репозиторий тут, ты же разработчик, иди работай. И это как раз, в принципе, и называется тот самый контекст. 

Контекст, который мы тоже хотим дать нашему агенту, чтобы он, независимо от каких-то наших дополнительных действий, продолжал делать хорошие результаты. В целом, в принципе, логично, чтобы понимать вообще, что происходит, давайте ему тоже такую инструкцию и дадим. Код не ломай, пиши хорошо, в общем, делай хорошо, и хорошо будет. 

Отлично. Для этого придумали, уж так появилось, зашло, файлик AgentsMD. Наверное, многие про него слышали.

В любом проекте он настроен. Мне вообще нравится подход Мартина Фаулера, он часто ссылается, что AgentsMD – это table of content. Я думаю, в принципе, если посмотреть какие-нибудь LM вики Карпатова, то любой такой входной индекс, как раз AgentsMD – это и есть, по сути, индекс нашего проекта, и является начало знания о нашем проекте, начало своей задачи.

Отлично. Мы знаем в целом, что происходит в проекте. Мы, наверное, хотим помнить, что вообще в этом проекте мы делали в рамках задачи, поэтому здесь возникает короткая память или тот самый event log, про который мы с вами говорили.

И мы хотим помнить, что мы вообще когда-то делали, долго memory, либо сохранить, либо вспомнить все эти раги, базы и тому подобное. Сразу возникает вопрос, где вообще этот контекст нам с вами хранить? Ну, мы можем попытаться сделать фиксированную структуру данных для нашего какого-то агента и его знаний и попытаться ее продумать изначально и сунуть куда-то в базу данных. К сожалению, опять же, наш впродовый опыт показал, что пользователи ожидают теперь от агента больше, нежели просто какое-то фиксированное знание и workflow. 

Поэтому логично, что нам необходимо правильно продумать организацию файлов в пространстве, в файловой системе, чтобы наш агент мог в ней ориентируясь, выполнять поставленную ему задачу. Ну, вот здесь на примере, как это выглядит у Гермеса. В принципе, тот же самый подход.

Мы посмотрели, что Гермес – это agent harness. У него точно так же есть какие-то файлики с OLMD, с его идентичностью. У него есть скиллы, EventLog он где-то хранит в StateDB, но это EventLog старых сессий. 

Ну и какую-то память он сохраняет в MemoryMD. То же самое плюс-минус в OpenCLO. В общем, любые харнесы теперь – это вот такой формат описания каких-то файлов.

Единственное, что мы здесь еще забыли, что нам помимо всех знаний в контекст мы еще кладем описание всех инструментов. Это очень важно, потому что, понятное дело, что в контексте модели наш агент, наш сотрудник должен вообще понимать, помимо того, что есть в организации, что он может делать. И в этом плане мне очень нравятся последние подходы, которые возникают.

Я не знаю, в России, наверное, не шибко слышал, но вот на западном рынке уже начинают к этому переходить. Например, EVE от Versely, это тоже такой очень удобный фреймворк. То есть, по сути, о чем они говорят? О том, что агент становится фиксированной структурой файлов, что бы он ни делал.

А вот как раз Agent Harness, там, где это все будет запускаться и работать, как бы Versely берет на себя инфраструктурно. Здесь, кстати, видно, что есть моделька, описанная у них в Agents, инструкции есть у каждого агента, тулзы. В общем, все описано именно статическими файлами подагенты.

В общем, все, что можно. Но возникает сразу вопрос, а разве мы можем вообще все запомнить? И контекст в модельке, он не резиновый. И вот так уж выходит, что наш сотрудник, он какой-то очень хитрый. 

Чем больше данных ему даешь, тем больше вводных даешь, тем дороже он нам обходится с вами каждый месяц. В целом, я, конечно, не знаю, рынок труда такое устраивает или нет. Мне кажется, надо ввести какие-то ограничения на таких сотрудников. 

Чем больше данных получает, тем больше мы должны им оплатить. Возникает сразу вопрос, как нам вообще все эти знания обработать? В принципе, в разных Harness'ах все по-разному пытаются обработать те или иные форматы данных. Ну, понятное дело, самый первый — это некоторая суммаризация.

Что такое суммаризация? Понятно, многие с этим сталкивались. Как только мы сильно много общаемся с нашим агентом или event log перерос, он начинает все это дело сокращать, пытаясь освободить какую-то память и увеличивает то самое контекстное окно, через которое мы ему пишем сообщение. Выглядит все это примерно так.

Мы это можем, опять же, настроить. Например, в плод коде можно сказать, суммаризуем на 80%, можно вообще не ставить лимиты, но тогда он, скорее всего, упадет по финалу. Отлично. 

Суммаризуем. Что еще можно? Можно добавить offload. То есть в чем идея? В том, чтобы наш агент Harness, он должен, когда получает сильно много данных, например, вызов от какого-то Tula, он, вместо того, чтобы весь его поместить в контекст и запомнить, мы ему четко в нашем плод коде, сколько я помню, 10 тысяч токенов. 

Если у вас из Tula приходит больше 10 тысяч токенов, он автоматически создает небольшой файлик. Что это позволяет? По сути, это как в пиратской рыбской аморе. У меня есть ключ, нет лучше.

У меня есть рисунок ключа. Примерно так агенты мы и делаем. У него висит рефа на результат Tula, и если надо, он потом в нее сходит.

Разумеется, чтобы сходить в это все дело и посмотреть, во всех Harness'ов есть прогрессив дисклоджер. Кстати, клод, как только добавили у себя этот прогрессив дисклоджер, у них контекст, экономика текста стала до 80%. Мы сейчас с вами еще про другие методы тоже поговорим.

Что это означает? Это означает, что мы в контекст не грузим сразу все, что можно. Мы скилы, Tula, в общем, любые сторонние данные, мы их оставляем вне нашего основного контекста, но при необходимости наш агент должен уметь найти эту информацию и получить ее. По сути, мы все время с вами держим компактный индекс. 

Ну, вот здесь я скинул как раз те самые скрины, как это выглядит в скилах и в MCP. То есть, если зайдете, и вам станет интересно, вы увидите, что MCP Tula теперь у клода, они у него хранятся просто по ссылке. Кстати, странно, что здесь токены не подписаны. 

Мне казалось, что там было раньше подписано по 10-20 токенов. Ну, в общем, все, что есть, он хранит как бы по ссылке и под агентов, и скилы, и не грузит все это в рантайме. Ну, кстати, скилы, да, это тот самый теперь он-демант загружаемый промпт, поэтому, я думаю, многие уже ими пользовались. 

В общем, точно такой же хороший пример progressive disclosure, который загружается при необходимости. Раз, два. И мы с вами что еще можем делать? Мы с вами можем оптимизировать ответ от Tulo.

То есть, делать это, опять же, на уровне таком дискретном, не пытаться анализировать, что именно он ответил, а пытаться как-то заоптимизировать, вырезать лишнее и оставить только нужное. Ну, например, я вот здесь вставил пример вызова РТК. Кто не пользовался РТК, знает такое? Rust Toolkit.

О, много рук. Я вот не помню, там еще какое-то одно выходило более популярное, что-то Handoff или как-то так называется, которое больше, чем РТК, может токенов экономить. Ну, вот здесь пример моего код-кода, у которого РТК стоит уже примерно за последние 8 недель. 

Я не помню, когда его поставил, но это результат за последние 8 недель. То есть, он мне сэкономил. Вместо 100 миллионов токенов, он оставил только 19 миллионов токенов.

Причем из них вот там все ушло, всякие грэппы, в Amazon что-то копировал, ленты, и все это понятно. Отлично. Ну, я, в принципе, кстати, вот как поставил РТК, мне кажется, мне стало подписки намного больше хватать.

И последний этот прошлый был такой более детерминированный подход. Мы много говорили, что есть как бы схематически больше, но мы можем попытаться именно оптимизировать ответы от модели. Для этого есть тоже разные скиллы. 

Некоторые говорят просто пишите ему Be Brief, вот это вот все. Последнее время сейчас стал популярный это тот самый Caveman скилл. Не знаю, пользовался кто-то или нет. 

Я поначалу поставил его. Он мне, в принципе, был окей, но через какое-то время я удивился. Думаю, как-то Клод странно пишет, как аббрегент.

Ходить сюда, сюда не ходить. Думаю, верну ему чуть более человеческое. В чем идея? В том, что в этом скилле прописано, чтобы Клод должен оставлять только важное, без всего этого, что он любит выдавать.

Как в примере. Баг в middleware проверить, меньше равно, больше равно. Но тоже многие говорят, что очень много экономит.

Но, если честно, надо прям привыкать к такому Клоду после всего этого дела. Получается, что контекст у нас — это некоторая инфраструктура, где мы ее должны хранить, мы можем знать, как мы ее должны обрабатывать, мы можем какие-то хаки придумывать для обработки. И контекст — это некоторая семантика, опять же. 

То есть мы можем оптимизировать, мы можем, вернее, просить модель по-другому ответить, мы можем подтягивать или просить ее подтянуть другие данные. В принципе, тот же самый паттерн повторяется. Отлично.

Получается, у нашего сотрудника есть рантайм, инструменты, контекст. И вроде все хорошо, но не хватает что есть в любой компании, в любой корпорации. Что есть? Корпоративная политика, все это. 

Должна быть цель, он должен идти к великому. Поэтому мы давайте ему сделаем еще и политики сверху. Ну, политики не в рамках, а пользист, как некоторые ограничения. 

Ну, какое? Самое популярное. Не очень-то хотели, чтобы наш агент, наш сотрудник выжрал все бюджеты и к нам не возвращался с результатом. Поэтому давайте явно ему объявим лимит на какие-то итерации, добавим бюджетирование.

Я думаю, скоро сюда добавится точно так же прямой доступ к деньгам. Я думаю, что у каждого агента или у каждого харнеса будет свой некоторый валет. Кстати, из последнего, Apify, я видел, что они уже такое тоже начинают добавлять.

В общем, кто слышал, все эти протоколы X402, ACP, MPP, в общем, все вот это вот, я думаю, тоже скоро добавится к бюджетированию. То есть мы нашему сотруднику будем не только ставить лимиты на итерации токены, будем ему еще говорить, больше пяти баксов не трать. Следующее, разумеется, все мы очень любим, когда мы поставили нашему стажеру задачу, он ее долго делает, или еще лучше делает ее быстро, но возвращается к нам уже с финальным результатом. 

Мы не хотим, чтобы он нас постоянно дергал, но тем не менее, если он где-то задуплил, мы хотим, чтобы он все-таки к нам вернулся и попросил от нас какое-то подтверждение. И опять же, здесь тоже такая классная стата, что вот этот human-in-the-loop, он нужен не для того, чтобы там вообще давайте harness делаем, человека полностью уберем, он будет сам там крутиться. Нет, разумеется, мы хотим, чтобы в каких-то решениях определенных агент все равно к нам возвращался, чтобы он, если понял, что, ой, извини, я что-то посмотрел, я поднял 40 серверов, я ожидаю, что он ко мне вернется и скажет, слушай, не посмотри, не удалил ли я чего лишнего? Или я, возможно, продаваю базу данных, что ты будешь делать со мной? Вот, уволю.

И, разумеется, некоторые guardrails. Guardrails тоже делятся по сути на два фактора. Один более детерминированный, второй более схематический. 

Ну, первый это, разумеется, давайте настроим, про который мы уже говорили, тестеры, линтеры, все возможные тайп-чеки, какой-то, может, структурный анализ, все, что дешево, мы в своих harness'ах пытаемся настроить вообще по максимуму. Чем больше мы настроим, тем проще нам будет валидировать задача. И, разумеется, guardrails такие уже более дорогие.

Когда агенту необходимо пройти ревью другого агента, когда мы применяем тот самый LMS judge, который должен подтвердить того, что наш pull request готов или не готов. Но эта вся вещь уже как раз про семантику, она подороже. Получается, что harness база состоит из четырех слоев. 

Это некоторый рантайм нашего агента, где он трудится и работает, это доступ к его инструментам, это правильно организованный контекст и это некоторые политики. Если мы перерисуем ее по той же схеме, про которую я говорил поначалу, что есть инфра, есть семантика, получается, что любая агентская обвязка она, по сути, является инфраструктурной частью, где есть рантайм и инструменты, которые где-то гоняются, и есть семантической частью. Сразу возникает вопрос, что нам с этим делать? Мы знаем, что есть отдельная инфра и есть семантика. 

Нам, как разработчикам, куда мы вообще с этим идем и что будет дальше? И мне здесь очень нравится, по-моему, это слайд от Бена Эванса с всего одного из недавних докладов, если я правильно называю его фамилию. Здесь количество строительства квадратуры дата центров по сравнению со строительством офисов в Америке. Мы явно видим, что где будут как раз обитать те самые наши Harness и наши сотрудники будущего.

Поэтому мы понимаем, что Harness — это не просто какая-то обвязка рук агента, а это корректно сформированное пространство, то самое нетоксичное пространство в компании, в команде, чтобы он мог выполнять поставленные ему задачи. Поэтому что нам необходимо? Нам уже сейчас необходимо проектировать задачи для наших агентов таким образом, ну и в принципе все его окружение, чтобы он мог их без нашего вмешательства прямого решать, потому что никто не любит микроменеджировать своих сотрудников, а особенно стажеров вот этих вот безбашенных. И все, что, скорее всего, к чему мы придем, все, что мы будем делать, я думаю, что будет выглядеть примерно вот так на рубеже ближайших пару лет. 

Раньше все говорили, что оно компилируется, а сейчас, наверное, все будут бегать, заниматься своими вещами и говорить, что оно там агентится. На этом у меня все. Большое спасибо.

Спасибо. Спасибо большое, Артем. Настало время вопросов.

Есть ли? Спасибо большое за доклад. Все-таки вопрос. Пытаться делать свои харнессы или все-таки глубоко копнуть в имеющиеся? Вот эту тему подраскрой, пожалуйста.

Я все-таки стою на том, что, поэтому я разделил, что есть инфра и семантика. Если с точки зрения делать свои харнессы, именно как инфраструктурную часть, делать свои тулы, свои лупы, идти настраивать коннекшн ко всяким разным инструментам, то я бы сказал, что здесь надо пользоваться тем, что уже есть на рынке. Ну и благо опенсорсных вещей, их очень много, поэтому мы даже, по сути, делаем свой контроль плейн для харнессов, и я лично могу назвать очень много опенсорсных конкурентов.

Буквально Databricks неделю назад выкатил тоже свою платформу по управлению харнессами. Поэтому я бы сказал, что мы не должны идти в написание именно инфровой части, нам необходимо ее поддерживать, но мы должны учиться именно правильно проектировать вот это пространство, чтобы агент потом в нем смог разобраться и семантически решить нашу задачу. Спасибо.

Есть еще? Спасибо. Очень действительно полезные вещи. А как ты бы предложил агенту дать еще возможность или это делается просто через MCP, иметь доступ к дополнительной информации? Это просто он сам идет, грубо говоря, к твоим каким-то базам знаний, к какому-то конференции или чему-то, или он, допустим, как-то через какой-то рак достает какую-то информацию, которая бы ему помогла в работе? Спасибо за вопрос.

Да, в идеале, понятное дело, что мы ему описываем способ решения задачи, то есть если мы ему явно не описали, сходи в такой-то рак, ну, если у него нет вообще информации, что ему надо куда-то сходить, и мы ему не описали явно, сначала проведи какой-то research, собери больше информации, найди практики, как это принято в индустрии, то, конечно, он этого не станет делать и пойдет все делать просто в лоб. То есть чем больше мы сможем ему описать то, именно, знаете, как по бизнес-процессу, как по workflow, что мне нравится подход, когда мы начинаем приступать к задаче, кажется многим, что давайте просто агенту напишем, он сделает. А как сделать хорошего агента? Давайте напишем, и он там сам напишет нам хорошего агента, которого делать. 

Нет. Мы идем, сначала пытаемся разобраться в бизнес-процессе, и как бы специалист решал эту задачу, и потом организуем пространство, чтобы наш агент так же решал. Если мы знаем, что специалист сначала посмотрел бы, какие есть базы данных, какие есть знания, ну, значит, надо это четко зафиксировать, либо там в скеле данный способ решения, ну, либо там вообще возможно сразу в AgentsMD. 

Ну, то есть мы не ожидаем оттого, чтобы он прошел все флоу, о котором мы у себя как бы в голове знаем, а ему не объяснили. Спасибо. А вот там я видел, да, молодой человек там был, да, он был вторым.

Спасибо большое за доклад. У меня такой вопрос. Когда мы говорим про сэндбоксы для агентов, кажется, что нужно делать какие-то гардрейлы для того, чтобы он там не сливал корпоративной политики, секреты или что-то еще. 

Вот какие подходы тут можно предложить в целом, чтобы обеспечить эти гардрейлы и LLM ко мне отправлять лишнего? Да, ну, я могу рассказать, каким подходом мы используем, просто он уже маленько у меня не влезал в доклад, поэтому я эту часть намеренно убрал. Мне вообще нравится подход такой, что гардрейлы, все говорят, что там security, security. Я считаю, что в первую очередь нам именно важен governance, то есть управление, и поэтому, по-моему, как раз, если здесь есть коллеги с GigaChat'а, да, там, со Сбера, вчера или позавчера Сбер выкатил свою платформу, там, EvoHarness или EvoClaw, она называется, по управлению харнесами.

И здесь совершенно так же по тому, по доступам, да, то есть мы не хотим пытаться заблокировать те действия, которые там агент пошел плохие делать по гардрейлу, мы хотим их сразу заранее переотвратить путем того, что мы просто на уровне вот этих вот governance, управления доступом, мы ему это не разрешим. Ну, мы, например, у себя рассмотрели несколько подходов, вот есть там рыбаки, абаки, все вот это вот, но эти системы не подходят. Мы используем подход Rebug Relation Based Access Control.

И, грубо говоря, у нас каждый permission или что агент, какой доступ, каким данным может получить, он вот на уровне вот этих вот связей контролируется, то есть там граф связи появляется. Это, если интересно, вы можете подробнее почитать. Сейчас два проекта есть в этой сфере основных, open-source'ных. 

Это от Ori Network, вот у них есть кета, и второй это Open FGA, по-моему, называется. Open FGA, что-то там такое. Вот там очень интересно как раз написано. 

В общем, ответ краткий, да, лучше настроить, пытаться не превратить через security, а лучше давайте будем делать governance и управлять, к чему агент имеет доступ. Спасибо. Так, вот там по молодому человеку в светлой футболке.

Спасибо за доклад. Вопрос такой. Ну, Harness, он, получается, это что-то универсальное, да, но при этом очевидно, что есть мощный опус какой-нибудь, и есть какой-то локальный сервачок на 2 мегабайта.

В зависимости от мощности модели Harness должен отличаться. Нужно ли это закладывать туда? Да, здесь, смотри, получается же, опус, да, модель она сама нам, по сути, не важна, потому что модель, она работает. Говорю, что агент, наш сотрудник, у него мозг лежит где-то там.

Мы же обвязку можем гонять, в принципе, хоть на девайсах. И поэтому я бы не сказал, что нам нужно как-то четко больше выделять, кроме случаев, когда ты знаешь, что у тебя агент может запускать какие-то высоконагруженные сервисы. Например, мы нашего приложения гоняем, разумеется, мы не поднимаем его на 2 CPU, 1 RAM, но мы стараемся ему этот стендбокс все кидать побольше, потому что мы хотим end-to-end его проверять, не микроменеджить, но чтобы он запустил, прошел по функциональным требованиям, посмотрел, прокликал сразу в браузере.

Конечно, тогда под такой флоу нужен побольше Harness. От модели же, получается, не зависит. Это уже в инфу часть идет, он остается.

Спасибо. Вот там сосед. Да, сосед.

Спасибо за доклад. Очень интересная тема сендбоксинга. Мне кажется, она может быть не совсем актуальна, когда ты работаешь как такой промпт-инжиниринг.

Ты сказал что-то сделать, агент сделал, там не такой большой разброс, что может агент натворить. Другая история, когда у тебя уже есть какой-то Harness, который ты собрал, он работает условно с Ralph Loop, и он уже может бесконечно работать, у него могут быть ограничения. Например, он делает какой-то ресерч, и мы ему выставили ограничения.

Умри, перестань работать, когда наступит 10 миллионов токенов суммарно в цикле ты потратишь, или там какой-нибудь тайм-аут. Какие есть варианты сендбоксинга для решения таких задач, потому что очевидно, агент может пойти в различные стороны, учитывая большое количество итераций. Очевидно, что может быть докер, но есть зафиксированные случаи, когда агент умел и выходить за границы сендбоксинга, используя уязвимости докера и так далее.

Что ты посоветуешь? Какой сендбоксинг использовать? Здесь интересно, потому что мы, по сути, свой имплементировали, но это ладно. Есть всегда желание написать что-то свое, особенно с веб-кодингом, но я бы, наверное, в это не лез. По сути, все сендбоксинги сейчас устроятся на плюс-минус готовых.

Есть A2B, он там строится на Firecracker от амазоновской технологии. Есть от Google на базе GVisor как раз. Есть всякие WASM-бейс сендбоксинги, которые стараются, чтобы ты все это делал.

Последнее, я, к сожалению, не помню точно, как называлась репа и от кого сделана. Я знаю, хорошее было от Alibaba. Выходил у них свой сендбокс.

И вторые ребята, я, к сожалению, забыл название, но они очень быстро поднимают сендбоксы. Мне в принципе сендбоксы нравятся те, которые в режиме Serverless работают. При необходимости он поднялся, задачу сделал и потом прекратил, например. 

Что-то на F, я не помню. В общем, они за, по-моему, 50 миллисекунд для тебя поднимают сендбокс. А теперь, получается, для агента это важно, поскольку он может в баш пойти, и нам все-таки для ответа очень важно, чтобы он быстро его поднял, а не по минуте ждал под в кубернетисе.

Ну, ладно, не минуты, по 10 секунд, по 30. Вот, поэтому, какие-то вот такие подходы, E2B, Daytona, в общем, все на open-source. Желательно open-source, желательно Apache.

Ну, или MIT. Надеюсь, ответил. Спасибо за вопрос.

Так, молодой человек в конце, ближе, с краю, да. Да, скажите, пожалуйста, как вы определяете границы ответственности каждого конкретного агента? То есть, как вы определяете, нужен один агент тут, или несколько, да, для разных классов задач? Как структурируете базы знаний для них? Ну, то есть, какая-то общая база знаний, да, для всех агентов, и какое-то там право доступа для них выдается на те или иные части? Или, то есть, для каждого какая-то копия, да, базы знаний, куда ему только нужно? Да, ну, здесь я могу сказать, я могу сразу ответить, что поскольку мы позиционируем всю эту систему политиков и доступов, как некоторое дерево, поэтому в зависимости от того, какой там, на базе relations данных сделано, какая связь есть, какой сущности. Например, у нас есть связь, как таска, да, и можно дать агенту, например, доступ в какой-то определенный инструмент, но ровно на один час.

Или дать ему доступ на вызов этого инструмента с определенным параметром. Ну, то есть, там довольно такой гранулярный набор, и как мы это структурируем? Ну, у разных агентов, понятное дело, есть свои разные инструменты, это все, в принципе, мне кажется, уже во многих системах настраивается. Но мы еще стараемся здесь в зависимости от того, какая identity его вызывала, да, то есть, как раз, если вызывал пользователь, то у него могут быть одни доступы еще на делегирование агентом. 

Если ему задачу дал другой агент через A2A, например, протокол, то у него может быть доступов меньше, потому что уже такая задача могла прилетать из какого-то неверифицируемого источника. Ну, то есть, в принципе, это настраиваемое вот как раз на базе вот этого вот подхода с Relation Based Access Permissions. Спасибо. 

Вопрос у девушки на седьмом ряду. Спасибо большое за доклад. Хотела поинтересоваться, я правильно понимаю, что если мы грамотно и жестко написали Harness, то мы можем спокойно вытащить, например, старую GPT-шку оттуда и ставить новую, и программа будет работать как бы без изменений? Старую GPU-шку? Нет, опять же, да, говорю, что Harness, он к GPU вообще не имеет никакого отношения, мне очень нравилось, когда первое время убежал клод-код, да, все такие убежал и агент клод-код, и многие вайб-кодеры пошли, такие, сейчас я у себя запущу этот Open Clod-код или что-то такое, назывался, а потом удивлялись, а почему это не работает? Ну, потому что мозг нашего агента гоняется на GPU, а GPU находится где-то вынесено. 

Поэтому, говорю, этих Harness, их по сути можно, от них зависит только одно, что агент доделает задачу, ну и какие данные он получит. Все остальное уже, как бы, там вызов самой Лоленке, он лежит за нашей гранью, если мы говорим про Harness. Поэтому, как бы, факт смены GPU, он никак здесь не влияет.

Мы можем даже в наш Harness пойти, другую модель поменять и посмотреть, лучше она станет работать или нет. Спасибо. Вот у молодого человека вопрос.

Здравствуйте, Михаил. Спасибо за доклад. Мне кажется, вот предыдущий вопрос, не уверен, что был GPU, там GPT бы тоже подошло.

Собственно, вопрос на предыдущем докладе, там одном из предыдущих говорили о том, что вот Сбер делал экосистему там вокруг моделей старых, да, слабых. Модели сильно шагнули и теперь им вот эта обвязка вся мешает. И вопрос собственно, как делать Harness таким образом, чтобы он не мешал модели в будущем, через полгода, через год. 

Или каким образом адаптироваться под это? То есть какие-то маркеры, какие-то не знаю, репетерии. Да, интересный вопрос. Мне просто кажется, вот здесь подход к Harness он еще приводит к маленько другому пути, в том плане, что мы сейчас пытаемся подоптимизировать все это дело под большую модель.

Но как только мы переходим к оптимизации Harness и оптимизации пространства под решение сдачи, мы сразу переходим уже ближе к вопросу SLM, то есть маленьких языковых моделей, чтобы они разбирались в отдельно взятом Harnessе и задачи лучше, чем их большие конкуренты. Поэтому я думаю, что здесь скорее все будет переходить Harness. В общем, интересный вопрос в том, что он мешает или не мешает. 

Потому что можно в кулларах объяснить, я знаю такие Harness, которые 50% своей работы отдают только на поддержание самого себя, а задачи не выполняют. Такие тоже есть. Поэтому мы стараемся правильно именно чтобы Harness был больше про семантику, а инфраструктурный уровень мы стараемся не поднимать наружу, чтобы не было такого, что агент должен все время спамить задачи, делать сам себе проекты, подпроекты.

В общем, на поддержание жизнеспособности самого Harnessа он не должен тратить много усилий. Надеюсь, ответил. Спасибо. 

И вот последний вопрос у молодого человека. Добрый день. Вопрос такой короткий. 

Какие метрики могли бы посоветовать, чтобы определить, что Harness хороший или плохой в качестве ключевых? Это хороший вопрос. Какие метрики посоветуешь? Ну, я бы сказал так. В рамках агентов нас в основном интересует task completion rate.

Если мы как ML инженеры, мы всегда заинтересованы в том, чтобы задача решалась end-to-end. Мы не хотим заглядывать внутрь того, как задача работает. Мы ожидаем, мы дали все спеки и вводные условия, результат получили.

Конечно, мы можем смотреть на количество потраченных токенов, если у нас стоит такая задача. Но я бы в первую очередь смотрел на task completion rate, потому что чем меньше размывается наше людское внимание, тем больше, скорее всего, задач мы можем делать в параллели. Поэтому я, когда смотрел бы в Harness, я бы именно ожидал, что он должен делать задачу, в первую очередь, делать задачу, а второе за меньшее количество шагов.

Это, кстати, интересно, потому что недавно было сравнение GLM, по-моему, и Клода. GLM 5.2, ей надо 80 шагов на выполнение той же самой задачи, когда Клоду надо 60. Я бы смотрел на task completion rate.

Мне кажется, это продовые метрики в большинстве таких систем. Спасибо.